# .env.integration - For integration tests (forked chain)

# Forked chain configuration
BASE_RPC_URL="http://localhost:8545"

# Test database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/relayer-test"
