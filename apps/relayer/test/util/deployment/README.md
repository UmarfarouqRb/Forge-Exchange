# Deployment Artifacts

This directory contains the deployment artifacts for the contracts.

- `addresses.json`: A JSON file containing the deployed contract addresses.
- `abi/`: A directory containing the ABI for each contract.
