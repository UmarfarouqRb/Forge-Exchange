import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <h1 style={{ color: 'white', fontFamily: 'sans-serif', fontSize: '3rem' }}>
      Hello from Forge!
    </h1>
  </React.StrictMode>
);
